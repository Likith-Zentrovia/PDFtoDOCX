# PDF to DOCX Converter Dependencies

# Core conversion library - handles PDF parsing and DOCX generation
pdf2docx>=0.5.8

# PDF processing (dependency of pdf2docx, but explicitly listed for clarity)
PyMuPDF>=1.24.0

# DOCX manipulation (for any post-processing needs)
python-docx>=1.1.0

# CLI interface
click>=8.1.0

# Progress bar for large files
tqdm>=4.66.0

# Image processing (for image optimization if needed)
Pillow>=10.0.0
